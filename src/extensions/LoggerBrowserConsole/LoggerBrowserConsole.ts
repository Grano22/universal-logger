export default class LoggerBrowserConsole 
{
    public register(loggerAccessor: any, extensionConfig: any): Record<string, any>
    {
        const proto = {

        };

        return proto;
    }
}