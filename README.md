# Universal Logger Alpha

Universal logger is a library designed for collecting and process logs in apps.

Usage:

```js
import Logger from 'universal-logger';

const logger = new Logger('test logger');
```